pages:
  stage: deploy
  script:
    - mkdir public
    - cp index.html styles.css script.js public/
    - cp -r assets public/assets
  artifacts:
    paths:
      - public
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
